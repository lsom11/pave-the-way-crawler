# Pave the way crawler

## Patterns used

This api is responsible for creating the scrapers we use to get user data. As a result we use the Factory pattern to create which scraper the user requests (LinkedIn, Facebook, etc.).
